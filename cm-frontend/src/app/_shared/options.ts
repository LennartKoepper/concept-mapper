export interface Options {
    filename: string,
    extension: string,
    context: string,
    model: string,
    temperature: number,
    num_nodes: number,
    show_node_props: boolean,
    show_edge_props: boolean,
    show_labels: boolean
}